import React from 'react';
import Screen from '../../misc_components/Screen';
import CustomText from '../../misc_components/CustomText';

export default function Camera() {
    return (
        <Screen>
            <CustomText text={"QR code scanner tab"} />
        </Screen>
    );
}